package com.blog.repository;

import com.blog.entity.Photpolio;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PhotpolioRepository extends JpaRepository<Photpolio,Integer> {
}
